import React from 'react'import PropTypes from 'prop-types'const items = [  { link: '#how', label: 'How does it works' },  { link: '#mission', label: 'Mission' },  { link: '#conditions', label: 'Conditions' },  { link: '#place', label: 'Place/withdraw' },  { link: '#distribution', label: 'Distribution' },  { link: '#contacts', label: 'Contact' }]const Nav = props => {  const listItems = items.map(({ link, label }) => (    <a key={link} href={link} className={`${props.type}__link`}>      {' '}      {label}{' '}    </a>  ))  return <nav className={`${props.type}`}>{listItems}</nav>}Nav.propTypes = { type: PropTypes.string }Nav.defaultProps = { type: '' }export default Nav